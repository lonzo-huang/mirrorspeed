/// singbox_flutter — sing-box 引擎的原生插件（安卓）。
///
/// Dart 侧不直接暴露 API：App 通过 `mirrorspeed/singbox` MethodChannel +
/// `mirrorspeed/singbox/stage` EventChannel 调用（见 client/lib/vpn/proxy_core_engine.dart）。
/// 本文件仅为插件注册占位。
library singbox_flutter;
